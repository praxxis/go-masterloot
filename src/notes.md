```
Controller
getItems
awardItem(slot, player)
openLootWindow

LootWindow(controller)
openItemWindow

ItemWindow(controller)

interface Items {
    lootSlot: number;
    link: string;
    name: string;
    quality: number;
}
```
